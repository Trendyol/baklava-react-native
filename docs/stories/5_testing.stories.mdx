import { Meta } from '@storybook/addon-docs';

<Meta
  title="Documentation/Testing"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
/>

# Baklava React Native Testing
> This guide will help contributors about how to test Baklava React Native.

### Brief
- Our coverage percentage for tests is `100%` with `npm run test:cov`.
- To run tests, you can simply type `npm run test` and run.
