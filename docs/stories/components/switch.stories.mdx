import { Meta } from '@storybook/addon-docs';
import Switch from '../assets/switch/switchDefault.png';
import LabeledSwitch from '../assets/switch/switchLabel.png';
import CustomizedSwitch from '../assets/switch/switchCustomized.png';
import DisabledSwitch from '../assets/switch/switchDisabled.png';

<Meta
  title="Components/Switch"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
/>

# Switch

Switch component can be used to toggle On or Off states of any single item.

<div
  style={{
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
  }}>
  <img src={Switch} width="250" alt="Switch" />
  <img src={LabeledSwitch} width="250" alt="Switch" />
  <img src={CustomizedSwitch} width="250" alt="Switch" />
  <img src={DisabledSwitch} width="250" alt="Switch" />
</div>

<br/>
<br/>

## Usage

```jsx
<Switch
    disabled={true}
    checked={false}
    customization={{colorOn: '#0BC15C', colorOff: '#FF5043'}}
    onChange={(value) => {console.log(value)}}
/>
```

<br />

## Props

### onChange

Type: `() => void`

Callback fired when the switch is checked

### label

Type: `string`

Optional label for the switch

### disabled

Type: `boolean`

Optional prop to make the component disabled

### checked

Type: `boolean`

Optional prop to make the component checked by default

#### customization

Type: `{colorOn: CSSProperties['color'], colorOff: CSSProperties['color']}`

To customize the color of the component
